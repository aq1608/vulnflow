# VulnFlow Enhanced - Complete Requirements
# All dependencies needed for VulnFlow + AI Enhancement
# Last updated: 2026-01-18

# ============================================================================
# CORE VULNFLOW DEPENDENCIES (Your existing project)
# ============================================================================

# Async HTTP client (REQUIRED for both original and enhanced scanner)
aiohttp>=3.8.0

# HTML parsing and web scraping
beautifulsoup4>=4.11.0

# HTTP requests library
requests>=2.28.0

# API server (if using VulnFlow API)
fastapi>=0.100.0
uvicorn>=0.22.0

# Data validation
pydantic>=2.0.0

# Command-line interface
click>=8.1.0

# Terminal formatting (optional but recommended)
rich>=13.0.0

# Template engine (for reports)
jinja2>=3.1.0

# ============================================================================
# ENHANCED SCANNER ADDITIONAL DEPENDENCIES
# ============================================================================

# NOTE: The enhanced scanner uses the same dependencies as original VulnFlow
# The only REQUIRED dependency is aiohttp (already listed above)
# No additional packages needed for core functionality!

# ============================================================================
# OPTIONAL DEPENDENCIES
# ============================================================================

# For development and testing (optional)
pytest>=7.3.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# For code formatting (optional)
black>=23.0.0
isort>=5.12.0

# For type checking (optional)
mypy>=1.3.0

# For documentation (optional)
mkdocs>=1.4.0
mkdocs-material>=9.0.0

# For secure API key prompts (optional, but recommended)
# Note: This is usually included in Python standard library
# but some systems may need it explicitly
# getpass  # Built-in, no install needed